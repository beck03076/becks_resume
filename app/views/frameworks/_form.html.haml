= form_for @framework do |f|
  - if @framework.errors.any?
    #error_explanation
      %h2= "#{pluralize(@framework.errors.count, "error")} prohibited this framework from being saved:"
      %ul
        - @framework.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
